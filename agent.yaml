# XRay-Distribute Agent配置
# 首次使用请运行: agent xray://token@host:port
# 配置会自动保存到此文件，后续直接运行 agent 即可

server:
  address: "http://your-server:8081"
  token: "your-secret-token"

proxy:
  listen: ":9090"
  cert_dir: "./certs"

log:
  level: "info"
  output: "stdout"
